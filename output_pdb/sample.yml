# 실제 분자 생성 단계(샘플링 단계)에서 필요한 설정들을 담당하는 파일

model:
  checkpoint: pretrained_models/pretrained_ipdiff.pt

sample:
  seed: 2024
  num_samples: 5
  num_steps: 10
  pos_only: False
  center_pos_mode: protein
  sample_num_atoms: prior
